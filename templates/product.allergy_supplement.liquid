<!-- /templates/product.allergy_supplement.liquid -->
{% render 'PawtectBlanket-css' %}
{% render 'all-supplement-css' %}
<link rel="stylesheet" data-href="{{ 'PawtectBlanket.css' | asset_url }}" />
<link rel="stylesheet" data-href="{{ 'all-supplement.css' | asset_url }}" />
<div class="pawtectBlanketProductContainer">
  <div class="peePad-header-content">
    <div class="container">
  	{% section 'product-allergy_supplement-header' %}
    </div>
  </div>
  <div class="blanketContent">
    {% section 'product-Allergy_supplement-middle' %}
  </div>
    <div class="blanketContent">
      <div class="container">
  {% section 'product-Allergy_Supplement-footer' %}
      </div>
  </div>
</div>



<script text="text/javascript">
  var _learnq = _learnq || [];

  var item = {
    Name: {{ product.title|json }},
    ProductID: {{ product.id|json }},
    Categories: {{ product.collections|map:'title'|json }},
    ImageURL: "https:{{ product.featured_image.src|img_url:'grande' }}",
    URL: "{{ shop.secure_url }}{{ product.url }}",
    Brand: {{ product.vendor|json }},
    Price: {{ product.price|money|json }},
    CompareAtPrice: {{ product.compare_at_price_max|money|json }}
  };

  _learnq.push(['track', 'Viewed Product', item]);
  _learnq.push(['trackViewedItem', {
    Title: item.Name,
    ItemId: item.ProductID,
    Categories: item.Categories,
    ImageUrl: item.ImageURL,
    Url: item.URL,
    Metadata: {
      Brand: item.Brand,
      Price: item.Price,
      CompareAtPrice: item.CompareAtPrice
    }
  }]);
</script>